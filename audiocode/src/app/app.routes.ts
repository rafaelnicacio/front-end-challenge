import { Routes } from '@angular/router';
import { TrackListPageComponent } from './components/track-list/track-list.component';

export const routes: Routes = [
	{ path: '', component: TrackListPageComponent }
];
